#include <stdio.h>
#include <include/input-format.h>

int main( int argc, char** argv ) {
    if (argc != 3) {
        return -1;
    }

    FILE* sourceImage = open_bmp(argv[1]);
    FILE*

    return 0;
}
